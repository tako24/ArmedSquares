using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScopeSystem : MonoBehaviour
{
    // [SerializeField] private GameObject scope;
    // [SerializeField] private FixedJoystick joystick;
    // [SerializeField] private float offset;
    //
    // public Vector3 GetScopeVector()
    // {
    //     var x = joystick.Horizontal;
    //     var y = joystick.Vertical;
    //     return offset * (Vector3)(new Vector2(x, y));
    // }
    //
    // private void Update()
    // {
    //     var x = joystick.Horizontal;
    //     var y = joystick.Vertical;
    //     scope.transform.localPosition = offset* (Vector3)(new Vector2(x,y));
    // }
}
